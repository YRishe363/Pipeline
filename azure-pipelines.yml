

trigger:
- none
pool:
  name: my-agent-pool

parameters:
- name: myname
  type: string
  default: name

- name: age
  type: string
  default: 0

variables:
  greet: "hello ${{ parameters.myname }}"

stages:
- stage: one_stage
  jobs:
  - job: one_job
    steps:
    - script: echo $(greet)

